# Markdown Parser

A parser for a subset of Markdown written in Elixir. 

This does not support all of the Markdown syntax yet, but hopefully it will some day. 
This is mainly a hobby project to get back into Elixir.
